<%- include('header') -%>

    <div class="box1" id="heading">

        <h1>Today</h1>
    </div>

    <div class="box">

        <% for(let item of store){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <div class="round">
                        <input type="checkbox" name="check" value="<%= item._id %>" onchange="this.form.submit()"
                            id="checkbox">

                        <label for="checkbox"></label>
                    </div>
                    <p>
                        <%= item.name %>
                    </p>
                    <br>
                </div>

            </form>
            <% } %>



                <form class="item" action="/" method="post">
                    <input type="text" name="item" placeholder="new" autofocus>
                    <hr>
                    <button id="b" type="submit" <span id="s2"></span>+</span></button>
                </form>
    </div>